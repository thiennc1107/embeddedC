gcc -o main $(mariadb_config --cflags) ./mySegway.c ./motors.c ./fileHandle.c ./sql.c $(mariadb_config --libs) -g -lwiringPi -lpthread -lm 

run with constPID
./main
run with webPID
./main -d
In this project , we create a 2 wheeled auto balancing robot by using MPU 6050 and LM298 motor driver, 
The program including connecting with MPU 6050 via I2c, then base on the y angle , we perform PID control to keep the angle 0 degree
after perform each PID control loop, we push the motor PWM data as speed data and the angle data and PID control error to web server database
In run with webPID constant, the program update PID parameter from web server . 